name: Refresh live Studio data and deploy website

on:
  workflow_dispatch:

permissions: {}

jobs:
  production-deploy:
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/studio-production-deploy.yml
    secrets:
      DUNE_API_KEY: ${{ secrets.DUNE_API_KEY }}
